<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.com.acevedo.carlos.showmeusers">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".ShowMeUsersApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_show_me_users"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_show_me_users_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.ShowMeUsers">
        <activity
            android:name=".components.userlist.activities.UserListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>